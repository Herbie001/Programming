#include <string>
#include <iostream>
#include <algorithm>
#include <vector>
using namespace std;

/**
 * Perform the BWT
 * @param str input string from user that ends in '$' char
 * @return String from concatenating last char of each string.
 */
string bwt(const string & str);
